// var platformName="cabp_server_admin_develop";

var oDate = new Date();
var systemData = {
	appId:1,
	version:'1.0',
	timestamp:oDate.getFullYear()+'-'+(oDate.getMonth()+1)+'-'+oDate.getDate()+' '+oDate.getHours()+':'+oDate.getMinutes()+':'+oDate.getSeconds(),
	appVersion:'1.0.0'
}